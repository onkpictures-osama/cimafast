[Service]
# Dramatic-structure report (dramaturgy_jobs.py): the preview's own spool,
# served by cimafast-dramaturgy-v1.service. Unset = the button is hidden, which
# is how production stays unchanged until the owner approves a worker there.
Environment=CIMAFAST_DRAMA_SPOOL=/var/lib/cimafast-v1/drama-jobs
