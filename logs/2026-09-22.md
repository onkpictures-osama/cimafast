# 2026-09-22

## Decisions

- **Daily archived logs.** Owner asked for daily archived logs of decisions
  and major milestones, kept on GitHub. → Created `logs/` in this repo (see
  `logs/README.md` for format and source). Infrastructure to wire a daily job
  that keeps it current automatically; this first entry was written by hand
  to seed the convention and back-fill the milestones already shipped.

## Milestones

Shipped 2026-09-21, carried into this log a day late since the log itself
started today:

- ✅ **F1** Companies, users and project access — on /v1. Accounts moved out
  of the secrets file into the database; a company admin manages their own
  team; two companies verified not to see each other's projects.
- ✅ **F2** Roles and permissions — on /v1. One rule table gates every write;
  viewers are read-only, only a company admin deletes a project.
- ✅ **F3** Audit trail and usage events — on /v1. Every change and every
  login/screen/export/AI-run event logged, scoped per company, viewable at
  `/v1/board/activity/`.
- ✅ **F4** Off-site encrypted backups — done. Nightly, encrypted on the
  server, pushed to the private `cimafast-backups` GitHub repo, restore-drilled.
- ✅ **H1** User home (concierge) — on /v1 at `/v1/home/`.
- ✅ **H2** Deep links — on /v1, `?project=`/`?tab=` with lazy tabs.

Owner decisions on record as of today (full detail in `PRODUCT-PLAN.md` →
"Owner decisions"):

1. Roadmap order: Phase 0 (ERP foundations) first — decided 2026-09-21.
2. Off-site backups: GitHub, documented — decided 2026-09-21.
3. Promoting /v1 to production: owner's call, on their timing — standing.
